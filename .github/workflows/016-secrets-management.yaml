name: Lab-016.yaml

on:
  push:
    branches:
      - main
      - mkdocs
      - gh-pages
    paths:
      - "Labs/016-secrets-management/**"
  pull_request:
    branches:
      - main
      - mkdocs
      - gh-pages
    paths:
      - "Labs/016-secrets-management/**"

jobs:
  build:
    runs-on: ubuntu-latest
    # Execute the step demo to verify that all is working
    steps:
      # Check out the code
      - uses: actions/checkout@v4

      - name: Test Lab
        working-directory: Labs/016-secrets-management
        run: bash ./_demo.sh
